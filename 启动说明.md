# 项目启动说明（qaq）

## 1) 启动 Milvus（数据库一）
在项目目录 `D:\AI Agent 项目\All-in-RAG\qaq` 执行：

```powershell
docker compose up -d
```

该命令会拉起：
- `milvus-standalone`（向量库）
- `milvus-etcd`
- `milvus-minio`

---

## 2) 启动 Neo4j（数据库二）
在 `D:\AI Agent 项目\All-in-RAG\qaq\data` 目录执行：

```powershell
docker compose up -d
```

---

## 3) 运行前端（由 FastAPI 静态托管）
前端不需要单独构建命令，启动后端即同时提供前端页面。

在项目目录执行：

```powershell
..\.venv\Scripts\python.exe -m uvicorn api.app:app --host 0.0.0.0 --port 8000
```

---

## 4) 访问链接
- 应用首页（前端）：[http://127.0.0.1:8000](http://127.0.0.1:8000)
- 健康检查接口：[http://127.0.0.1:8000/api/health](http://127.0.0.1:8000/api/health)
- Neo4j Browser：[http://127.0.0.1:7474](http://127.0.0.1:7474)
- Milvus 健康检查：[http://127.0.0.1:9091/healthz](http://127.0.0.1:9091/healthz)
- MinIO Console：[http://127.0.0.1:9001](http://127.0.0.1:9001)

---

## 5) 一键启动（推荐）
可直接运行：

```powershell
start_all.bat
```

该脚本会自动：
1. 启动 Milvus
2. 启动 Neo4j
3. 检查并安装 Python 依赖（缺失时）
4. 启动 FastAPI（同时提供前端）

---

## 6) 运行菜谱解析 Agent

默认只生成可审查产物，不修改 Neo4j：

```powershell
..\.venv\Scripts\python.exe scripts/parse_recipes.py `
  --input data/dishes `
  --output artifacts/recipe_agent `
  --limit 3
```

解析指定文件时可以重复传入 `--file`。断点续传使用 `--resume`；需要在保留其他产物的同时重跑指定文件时增加 `--force`。确认产物后，增加 `--apply` 才会逐菜谱增量写入 Neo4j：

```powershell
..\.venv\Scripts\python.exe scripts/parse_recipes.py --file data/dishes/aquatic/红烧鱼.md --resume --apply
```

产物包括 `recipes.jsonl`、`failures.jsonl`、`agent_traces.jsonl`、Neo4j 兼容 CSV 和汇总报告。Agent 只允许调用 Markdown 拆分、用量规范化、结构校验和最终提交四类本地工具。

---

## 7) 运行检索评测

Neo4j、Milvus 和 DeepSeek API 均可用时执行：

```powershell
..\.venv\Scripts\python.exe scripts/evaluate_retrieval.py
```

评测会比较纯向量、BM25 + 向量和完整多路加权 RRF，输出 Recall@5、MRR@5、平均延迟与逐题命中记录到 `artifacts/evaluation`。默认使用 `k=60`，并通过 `RRF_BM25_WEIGHT`、`RRF_VECTOR_WEIGHT`、`RRF_ENTITY_TOPIC_WEIGHT`、`RRF_GRAPH_WEIGHT` 调整通道权重。

当前 12 个样例的实测 Recall@5 为：纯向量 `0.8403`、BM25 + 向量 `0.9236`、完整多路 `0.9514`。完整结果以 `artifacts/evaluation/report.md` 和 `results.json` 为准。

---

## 8) 运行自动化测试

```powershell
..\.venv\Scripts\python.exe -m unittest discover -s tests -v
```
